const Footer = () => {
  return (
    <div
      className="footer-content"
      style={{
        textAlign: "right",
        padding: "1rem",
        color: "#888",
        fontSize: "12px",
      }}
    >
      OpenCost UI
    </div>
  );
};

export default Footer;
